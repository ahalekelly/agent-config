- [ ] Figure out how to get Amazon and ebay product data
- [ ] make amazon links generated by this tool affiliate links so that we can hit the 10 orders per month
- [ ] add Walmart, Target, Best Buy, Etsy
- [ ] Keep agent config in sync across macOS/Linux/Windows: Pi on Linux has no AGENTS.md (`home/.pi/agent/AGENTS.md` symlinks to the Mac path `/Users/akelly/...`; `setup-linux.sh` only swaps `.claude`), and machine-specific notes live only in `home-linux/.claude/CLAUDE.md` "Linux Notes", so Pi never sees them (e.g. headless/GFXOFF, headed browser on Xvfb with `--disable-gpu`, `~/power-monitor/`). Need one place for per-machine notes that every agent reads on every OS, and setup scripts that wire all agents (not just Claude) identically.
