- [ ] Figure out how to get Amazon and ebay product data
- [ ] make amazon links generated by this tool affiliate links so that we can hit the 10 orders per month
- [ ] add Walmart, Target, Best Buy, Etsy
- [ ] akelly-desktop: add the prevent-root-search hook to `~/.claude/settings.local.json` (PreToolUse, matcher `Bash`, command `uv run ~/.agents/hooks/prevent-root-search.py`); it was removed from the shared `claude/settings.json` on 2026-09-14 and the desktop has no root-search guard until this is done
