{
  "description": "Adrian's tweaks: pinned tools, trimmed standing context, instructions scoped to a model family or role, a sandbox policy cut to the paths a task acts on, hook names dropped from attached context, labelled cron prompts, usage context, todo: capture, task-notification provenance, the spawn model guard and the read size guard",
  "modules": ["./register.ts"]
}
